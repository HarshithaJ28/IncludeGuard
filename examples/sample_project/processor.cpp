#include "utils.h"
#include <regex>     // Expensive and unused!
#include <boost/algorithm/string.hpp>  // Not even installed, but checking

void processStrings() {
    // This function doesn't actually use regex or boost
    // but includes them anyway
}
